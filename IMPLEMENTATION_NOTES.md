# 实现说明（P0）

## 关键交互规则
- 提问标题限制 1-200 字。
- 标签数量限制 1-5。
- 同一问题最多只有一个采纳答案。
- 投票支持重复点击撤销。
- 关注问题后，新增回答会标记为“有新回答”；进入详情后清除标记。
- 页面筛选状态通过 URL Query 同步。

## 设计与实现取舍
- UI 结构优先复用 Figma 参考思路；功能规则严格按 PRD P0。
- 使用 Context + Reducer 管理内存态，便于后续迁移到 API 层。
- 搜索建议/历史记录归入 P1，不在本次范围。